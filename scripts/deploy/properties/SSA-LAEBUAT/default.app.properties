#WarFileNameBuiltByJenkins,DestinationWarFileName,DestinationAppNameAfterExtract,ETL(Note:Put ETL if ETLRepository deployment is needed,Rules (Note: Put Rules if RulesRpository deployment is needed);
APP1=enportal-webapp.war,LASelfService.war,LASelfService;